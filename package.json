{
  "name": "viaticos-vh",
  "version": "1.0.0",
  "private": true,
  "description": "Viáticos VH — gestión de anticipos y legalización",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "node scripts/dev.ts",
    "build": "npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend start",
    "usuario": "npm --prefix backend run usuario --",
    "seed": "npm --prefix backend run seed",
    "test": "npm --prefix backend test",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck"
  },
  "engines": {
    "node": ">=22.18"
  },
  "type": "module"
}
